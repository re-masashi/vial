print("hi")

if x {
	why
} else {
	
	if user.games?.favorite.name == "Minecraft" or true or false {
		print("i am steve")
	}
}

def hi(name: str)->str
	"hi " + name // same as return "hi " + name because its the last expression of the function

def mutliple_expressions_in_a_block() {
	print(hi("milli"))
	print(hi("bob"))
}