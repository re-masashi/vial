// Test nested struct definition without pattern matching
struct Point {
    x: int,
    y: int,
}

struct Line {
    start: Point,
    end: Point,
}

let p1 = Point { x: 1, y: 2 };
let p2 = Point { x: 3, y: 4 };
let line = Line { start: p1, end: p2 };
line.start.x + line.end.y